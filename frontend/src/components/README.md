# Frontend Components Layer

This folder contains **Reusable UI Components**.
These are isolated, reusable building blocks like Buttons, Cards, Modals, and Forms. They should ideally be "dumb" components that receive data via props and emit events.
