# Backend Controllers Layer

This folder contains the **Routing and Request Handling** layer of the backend application.
Following the **Skinny Controllers** principle, controllers here should only handle incoming HTTP requests, validate input, and delegate business logic to the `Services` layer.
