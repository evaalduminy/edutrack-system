# Backend Repositories Layer

This folder contains the **Data Access** layer of the application.
Repositories abstract the database operations (using Eloquent Models), keeping the query logic centralized and making the codebase easier to test and maintain.
